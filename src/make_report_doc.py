"""生成《HP-GNN 复现报告》docx。

一次性脚本。所有数字来自 data/processed/ 下的实验日志与诊断脚本输出。
"""
from docx import Document
from docx.shared import Pt, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn

OUT = r'd:\hp-gnn-pinn-epilepsy\复现报告.docx'

doc = Document()
st = doc.styles['Normal']
st.font.name = 'Calibri'
st.font.size = Pt(10.5)
st.element.rPr.rFonts.set(qn('w:eastAsia'), '等线')


def set_font(run, ascii_font='Calibri', ea_font='等线'):
    run.font.name = ascii_font
    rPr = run._element.get_or_add_rPr()
    rPr.get_or_add_rFonts().set(qn('w:eastAsia'), ea_font)


def h(text, level=1):
    p = doc.add_heading(text, level=level)
    for r in p.runs:
        set_font(r, 'Calibri', '微软雅黑')
        r.font.color.rgb = RGBColor(0x1F, 0x35, 0x64)
    return p


def para(text, bold=False, italic=False):
    p = doc.add_paragraph()
    r = p.add_run(text)
    r.bold = bold
    r.italic = italic
    set_font(r)
    return p


def bullet(text):
    p = doc.add_paragraph(text, style='List Bullet')
    p.paragraph_format.left_indent = Pt(18)
    for r in p.runs:
        set_font(r)
    return p


def formula(text):
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r = p.add_run(text)
    r.font.name = 'Cambria Math'
    r.italic = True
    rPr = r._element.get_or_add_rPr()
    rPr.get_or_add_rFonts().set(qn('w:eastAsia'), '等线')
    return p


def table(headers, rows):
    t = doc.add_table(rows=1, cols=len(headers))
    t.style = 'Table Grid'
    for i, htxt in enumerate(headers):
        cell = t.rows[0].cells[i]
        cell.text = ''
        r = cell.paragraphs[0].add_run(htxt)
        r.bold = True
        r.font.size = Pt(9.5)
        set_font(r)
    for row in rows:
        cells = t.add_row().cells
        for i, v in enumerate(row):
            cells[i].text = ''
            r = cells[i].paragraphs[0].add_run(str(v))
            r.font.size = Pt(9.5)
            set_font(r)
    doc.add_paragraph()
    return t


# ============================================================
title = doc.add_heading('HP-GNN 癫痫发作预测模型复现报告', level=0)
for r in title.runs:
    set_font(r, 'Calibri', '微软雅黑')

sub = doc.add_paragraph()
sub.alignment = WD_ALIGN_PARAGRAPH.CENTER
r = sub.add_run('复现对象：Physics-informed graph neural networks for robust '
                'cross-patient epileptic\nseizure prediction via chimera state detection\n'
                'Masoud Amiri, Ershad Nedaei, Bahador Makkiabadi\n'
                'PLoS ONE 21(4): e0345470, 2026　DOI 10.1371/journal.pone.0345470　PMID: 41926489\n'
                '数据集：CHB-MIT Scalp EEG Database（本复现仅用 chb01）\n'
                '本报告日期：2026-09-14')
r.font.size = Pt(10)
r.italic = True
set_font(r)

# ------------------------------------------------------------
h('摘要', 1)
para('本工作复现了 Amiri 等（2026）提出的癫痫发作预测模型 HP-GNN。该模型的核心主张有两项：'
     '一是用超图卷积刻画多通道脑电的高阶耦合结构，二是用 Kuramoto 方程的物理残差'
     '约束相位演化（物理信息神经网络，PINN）。')

para('复现过程中发现，论文补充材料中的官方源码无法直接运行，且缺少关键的预处理模块。'
     '本工作自行补全了从原始 EDF 到超图输入的完整数据管线，修正了物理损失的实现，'
     '并在 CHB-MIT 数据集上完成了训练与评估。')

para('主要结论有四条：', bold=True)
bullet('官方源码的物理损失项在实现上退化：导数项被写死为零，'
       '且与论文正文「用有限差分近似 dθ/dt」的表述直接冲突。'
       '实际效果是把耦合强度压向零。')
bullet('论文自报的物理损失效应量（+0.9%），小于其自身的折间标准差（±2.1%）。'
       '本复现的对照实验测得 +0.36%，与论文一致。'
       '因此结论不是「没能复现出该效果」，而是该量级本就落在噪声内。')
bullet('进一步的检验表明，问题不止于实现。观测到的 Hilbert 相位并不服从 Kuramoto 动力学：'
       '在 27087 个窗口的拟合中，耦合项对相位演化的方差解释率最高仅 1.55%，'
       '发作间期低至 0.00%。即便完全正确地实现，该损失项也不可能提供有效约束。')
bullet('事件级评估得到 100% 灵敏度，但这一结果是平凡的——模型在正常序列上的误报率'
       '为 7.5%，而单次发作的预测窗口包含数百条序列，命中接近必然。'
       '有意义的指标是误报率：本复现为 0.64 次/小时，论文报告为 0.48 次/小时。')

# ------------------------------------------------------------
h('一、复现目标与范围', 1)

para('1.1 任务定义', bold=True)
para('脑电癫痫发作预测的目标，是在发作发生前的一段预警期内给出报警，'
     '同时把正常时段的误报控制在可接受水平。按论文定义，'
     '发作前 30 分钟被标为发作前期（preictal），其余时段分为发作期、'
     '发作后 30 分钟、以及发作间期。')

para('1.2 本复现的范围', bold=True)
para('论文使用了三个数据集：CHB-MIT（22 名儿科患者、182 次发作、844 小时）、'
     'IEEG.org（16 名成人、87 次发作、312 小时）与 SIENA（14 名成人、186 小时）。'
     '表 7 给出三者的事件级结果：准确率 84.7% / 79.2% / 82.3%，'
     '灵敏度 89.3% / 83.1% / 86.7%，误报率 0.48 / 0.62 / 0.54 次每小时。'
     '需要说明的是，SIENA 并未出现在论文摘要中，'
     '只在方法节与表 7 里作为补充验证出现。')

para('受计算资源限制（单卡 RTX 5060 Laptop，8 GB 显存），本复现选择在 '
     'CHB-MIT 数据集的 chb01 单患者上完成全流程。chb01 含 42 个 EDF 文件、'
     '23 个电极通道、7 次发作。测试集由每 5 个文件取 1 构成（9 个文件、'
     '8.65 小时、3 次发作）。')

para('因此本复现与论文之间不存在可比性上的对等关系：论文报的是跨患者结果，'
     '本复现是单患者结果；论文的事件级指标建立在 182 次发作之上，'
     '本复现只有 3 次。这一点在第八、九节展开。')

para('需要说明的是，单患者的发作事件数过少（测试集仅 3 次），'
     '事件级指标无法与论文做统计比较。这一点在第八节讨论中展开。')

# ------------------------------------------------------------
h('二、论文方法概述', 1)

para('HP-GNN 的流程可拆为四步：')

para('2.1 超图构建', bold=True)
para('对每个 5 秒窗口的 22 通道脑电，用 Hilbert 变换提取瞬时相位：')
formula('φ_c(t) = angle( hilbert( x_c(t) ) )')
para('再计算通道间的相位锁定值（PLV）作为耦合强度：')
formula('PLV_ij = | (1/T) Σ_t exp( i(φ_i(t) − φ_j(t)) ) |')
para('PLV 超过阈值 τ 的三个通道构成一条 3-团超边，全部超边构成超图。'
     '论文给出 τ = 0.65（由网格搜索确定），本复现改用 τ = 0.55，'
     '这是一处有意的偏离，理由见 4.2 节。')

para('2.2 超图卷积与序列建模', bold=True)
para('由超边构造关联矩阵 H，经度归一化得到超图卷积所需的传播矩阵 P。'
     '每个窗口的特征为 22×22 的 PLV 矩阵（取上三角展平为 231 维）。'
     '窗口按时间排成长度 10 的序列，经超图卷积编码、图池化后送入 GRU，'
     '最后接两个输出头：四分类状态头与发作时间回归头。')

para('2.3 物理信息约束（本复现的重点）', bold=True)
para('论文用 Kuramoto 方程约束相位演化：')
formula('dθ_i/dt = ω_i + (K/N) Σ_j P_ij · sin( θ_j − θ_i − α )')
para('网络额外输出相位 θ、本征频率 ω、耦合强度 K 三个量，'
     '物理损失定义为方程两边残差的均方误差，以权重 λ 加入总损失：')
formula('L = 0.8·L_state + 0.5·L_time + λ·L_physics')
para('论文取 λ = 0.03。')

# ------------------------------------------------------------
h('三、官方源码审读', 1)

para('论文补充材料中共有 4 个 Python 文件（code_1、code_3、code_4、code_5）。'
     '逐行审读后发现三类问题，完整的证据链已另文整理（见《官方源码问题汇总.docx》），'
     '此处摘要。')

para('3.1 关键模块缺失', bold=True)
para('编号从 code_1 直接跳到 code_3，code_2 完全不存在。'
     '而 code_1 做的是信号清洗（带通、ICA、标准化、CAR），'
     'code_3 需要的是超图输入，两者之间恰好缺了「Hilbert 相位 → PLV → 超边」'
     '这一步。这正是 code_2 本该完成的工作。'
     '对全部源码检索「code_2」零匹配，说明它从未被包含在补充材料中。')

para('3.2 物理损失实现退化', bold=True)
para('源码第 249 行将导数项写死为零：')
formula('dtheta_dt = torch.zeros_like(phases)')
para('随后第 264 行计算残差均方误差。由于一边恒为零，'
     '该损失退化为 mean( rhs² )，其最小值在 ω → 0、K → 0 处取得。'
     '也就是说，论文声称的物理约束，实际效果是一个把耦合强度压向零的正则项。'
     '源码第 248 行还留有作者自己的注释：「In practice, use consecutive time windows」。')

para('此外，源码第 254-261 行的相互作用项对所有通道等权求和，'
     '完全没有使用超图矩阵 P。论文的两个核心卖点——超图结构与物理约束——'
     '在实现中没有任何联系。')

para('3.3 数据从未被加载', bold=True)
para('全部四个源码文件中没有任何一处读取 CHB-MIT 数据。'
     '训练脚本的输入由 np.random.randn 生成，标签由 torch.randint 生成，'
     '超边被硬编码为固定的 (0, 1, 2)，邻接矩阵被设为单位阵。'
     '换言之，官方代码从未在真实数据上运行过。')

# ------------------------------------------------------------
h('四、本复现补全的数据管线', 1)

para('由于 code_2 缺失且 code_1 未接真实数据，本节所述的模块全部为自行实现。')

table(
    ['自建模块', '功能', '关键参数'],
    [
        ['plv.py', 'Hilbert 变换、瞬时相位、PLV 计算', '—'],
        ['hypergraph.py', '逐窗 PLV → 3-团超边，坏道剔除', 'τ = 0.55'],
        ['build_prop.py', '关联矩阵 H → 度归一化 → 传播矩阵 P', '—'],
        ['build_timeline.py', '跨文件全局时间轴、发作时刻映射', '—'],
        ['build_windows.py', '5 秒窗口切分、四类状态标签', 'preictal = 30 min'],
        ['dataset.py', '序列构造、按文件划分训练/测试集', 'T = 10，stride = 1'],
    ])

para('4.1 通道对齐', bold=True)
para('CHB-MIT 的部分记录含 23 个通道，其中一个（T8-P8-1）与 T8-P8 完全重复。'
     '本复现按与官方一致的做法，在检测到重复时剔除该通道，统一为 22 通道。')

para('4.2 τ 阈值的确定', bold=True)
para('论文给出 PLV 阈值 τ = 0.65，用于 3-团超边检测。'
     '本复现在实现中采用了 τ = 0.55 —— 这是一个有意的偏离，'
     '原因是在本复现的流程下，τ = 0.65 会使超边数量过少、超图过于稀疏。')
para('偏离的可能机制是预处理差异。论文的 code_1 包含 1-50 Hz 带通、'
     'FastICA 去伪迹、稳健标准化与 CAR；本复现为简化流程，'
     '直接使用原始信号提取相位。PLV 的绝对水平依赖信号的频谱成分与噪声，'
     '省略这些步骤会系统性改变 PLV 的分布，最优阈值也随之偏移。')
para('本复现未对该阈值做敏感性分析，也未复现论文的完整预处理链。'
     '这是本研究的一处方法学偏离，在解释与论文的数值差异时应当计入。')

para('4.3 数据划分', bold=True)
para('按文件划分：每 5 个文件取 1 个作为测试集。'
     '这一划分保证了同一段记录的不同窗口不会同时出现在训练集和测试集中。'
     '最终得到 28778 条序列，其中测试集 6144 条。')

# ------------------------------------------------------------
h('五、模型与训练', 1)

para('模型结构参照论文描述实现：')
bullet('超图卷积层：22 → 64 维（使用传播矩阵 P）')
bullet('图池化 + GRU（隐藏维 64）')
bullet('四分类状态头（发作间期 / 发作前期 / 发作期 / 发作后）')
bullet('发作时间回归头（ReLU 保证非负）')
bullet('Kuramoto 物理模块')

para('总参数量 30917。优化器与训练轮数：20 轮，'
     '损失权重 0.8·L_state + 0.5·L_time + λ·L_physics。')

para('与论文的三处结构性差异（均为有意的简化，需在比较时计入）：', bold=True)
bullet('时序模型：论文使用 Mamba 状态空间网络（线性复杂度 O(T)），'
       '本复现以 GRU 替代，原因是 Mamba 需要额外依赖且显存开销较大。')
bullet('超图卷积层数：论文为三层（64 → 128 → 256 维），'
       '本复现为单层 22 → 64 维，以适配 8 GB 显存。')
bullet('训练流程：论文采用两阶段（844 小时自监督预训练 + 有监督微调）'
       '与四折交叉验证；本复现为单阶段有监督训练，未做交叉验证。')

# ------------------------------------------------------------
h('六、实验结果', 1)

para('6.1 物理损失对照实验', bold=True)
para('为隔离物理损失的作用，设计了两组对照：除 λ 之外，'
     '随机种子、数据洗牌顺序、模型架构、训练轮数完全一致。')

table(
    ['指标（后 10 轮均值）', 'λ = 0（基线）', 'λ = 0.03（物理版）', '差值'],
    [
        ['总准确率', '0.7180', '0.7216', '+0.0036'],
        ['Balanced accuracy', '0.6438', '0.6503', '+0.0065'],
        ['类1（发作前期）recall', '0.5187', '0.5058', '−0.0129'],
        ['类1（发作前期）precision', '0.2300', '0.2240', '−0.0060'],
        ['类3（发作期）recall', '0.5435', '0.5468', '+0.0033'],
        ['类3（发作期）precision', '0.5038', '0.5171', '+0.0133'],
    ])
para('单轮准确率的波动范围达 0.124，两组之间的差值 0.0036 是它的三分之一。', bold=True)

para('这个结果需要与论文自报的数字对照。论文方法节的消融研究原文为：')
formula('Physics constraints (+0.9%) improve generalization despite small loss weight (λ1 = 0.03)')
para('即论文自己承认，物理损失只带来 0.9% 的准确率提升。'
     '而论文表 5 报告 HP-GNN 的折间标准差为 ±2.1%。'
     '两相对照：论文自报的效应量 0.9%，小于它自己的折间噪声 2.1%。')

para('本复现测得的 +0.36% 与论文的 +0.9% 处于同一量级，方向也一致。'
     '因此本复现与论文之间并不存在矛盾——'
     '分歧在于论文把这一量级的效应解释成了有效的物理约束。', bold=True)

para('需要说明的是，论文未报告该消融项的配对差值标准差，'
     '因此无法从原文判断其声称的 p < 0.05 是否成立；'
     '但可以确定的是，0.9% 这一效应量小于论文自己报告的性能波动。')

para('6.2 物理模块内部状态', bold=True)
para('训练过程中记录了物理模块输出的三个量：')
table(
    ['轮次', 'L_physics', '耦合强度 K', '本征频率 ω', '|dθ/dt|'],
    [
        ['1', '0.0089', '0.8721', '0.1417', '0.0594'],
        ['5', '0.0006', '0.8115', '0.0805', '0.0654'],
        ['10', '0.0003', '0.5304', '0.0717', '0.0629'],
        ['15', '0.0002', '0.2677', '0.0604', '0.0570'],
        ['20', '0.0002', '0.2194', '0.0470', '0.0451'],
    ])
para('三个量在 20 轮内同步单调下降，且 K 到训练结束时仍未收敛。')
para('这一现象可以从损失函数直接推出。物理残差方程的两边——dθ/dt 与 ω——'
     '全部是网络的自由输出，没有任何一项来自观测数据。'
     '因此 ω = 0、K = 0、dθ/dt = 0 是方程的平凡解，残差恰为零。'
     '进一步地，ω 经 softplus 激活恒为非负，而 dθ/dt 可正可负，'
     '对于在零附近对称摆动的 dθ/dt，使残差最小的非负 ω 就是零。'
     '网络向这个方向收敛是数学上的必然。', bold=True)
para('值得注意的是，论文源码将 dtheta_dt 写死为零的版本，'
     '与本复现用真实有限差分实现的版本，全局最优解完全相同，'
     '只是本复现的收敛路径更慢。')

para('这里有一处可以直接与论文对照的推论。论文报告其学到的耦合强度为：')
formula('K = 0.71 ± 0.14 —— falls precisely within the theoretical chimera regime [0.4-0.9]')
para('而本复现中 K 从 0.8721 一路降至 0.2194，已经落在该区间之外。'
     '换言之，正确实现这个物理损失，反而把 K 推出了论文声称找到它的那个区间。'
     '这与论文源码版本（K 同样压向零）在方向上是完全一致的。', bold=True)

# ------------------------------------------------------------
h('七、Kuramoto 假设的检验（本工作的主要发现）', 1)

para('第六节说明的是实现层面的问题。但即便把 dtheta_dt 正确实现，'
     '物理损失能否起作用，还取决于一个更基本的前提：'
     '观测到的相位是否真的服从 Kuramoto 动力学。'
     '如果数据本身不服从该方程，那么把网络约束到方程上只会得到更差的拟合。'
     '本节对此做了直接检验。')

para('7.1 检验方法', bold=True)
para('对真实脑电信号提取 Hilbert 相位后，按最小二乘拟合完整的 Kuramoto 方程：')
formula('Δθ_i = ω_i·Δt + K·Δt·Σ_j sin( θ_j − θ_i − α )')
para('其中 α 为相位延迟。论文将该参数限制在 [0, π/2] 并报告其学习值为 0.28 rad，'
     '但未给出与本检验可比的拟合设定，因此本复现对 α 在 [−π, π] 上扫描 25 个值，'
     '取统计量最大的一个（这对原假设最有利）。'
     '评价指标为耦合项带来的残差方差降幅——即该方程能解释多少相位演化。')

para('需要说明检验的技术细节：相位必须在整段信号上 unwrap 后再按窗口采样，'
     '否则 6 Hz 振子在 5 秒内累积约 30 个周期，差分取模后会丢失全部频率信息。'
     '本复现最初的两次检验设计（逐窗取圆均值、以及用常数模型计算 R²）'
     '均存在此类缺陷，最终结果以下表为准。')

para('7.2 检验结果', bold=True)
para('在 chb01 全部 42 个文件上，按脑状态分别拟合：')
table(
    ['状态', '窗口数', '未滤波', 'theta (4-8 Hz)', 'alpha (8-13 Hz)'],
    [
        ['发作间期', '27087', '|t|=4.46  降幅 0.00%', '|t|=1.71  降幅 0.01%', '|t|=2.45  降幅 0.00%'],
        ['发作前期', '1964', '|t|=1.27  降幅 0.03%', '|t|=0.69  降幅 0.00%', '|t|=1.19  降幅 0.05%'],
        ['发作期', '88', '|t|=0.72  降幅 0.17%', '|t|=1.01  降幅 0.04%', '|t|=1.11  降幅 1.55%'],
    ])
para('（"降幅"指加入耦合项后残差方差的下降比例）')

para('7.3 结论', bold=True)
para('关键指标是降幅而非 |t|。发作间期一列出现了 |t| = 4.46 的"显著"结果，'
     '但它对应的方差解释率是 0.00%——在 27087 个样本下，'
     '任何微小的非零效应都会达到统计显著。这是统计显著与实际显著分离的典型情形。')

para('把三个脑状态分开检验，是因为癫痫发作本身是一种同步现象，'
     'Kuramoto 模型在发作期最有可能成立，这是对本假设最有利的情形。'
     '结果显示发作期的方差解释率最高为 1.55%（alpha 频带），'
     '且该状态的窗口数仅 88 个（7 次发作合计 7.9 分钟），样本量过小。')

para('据此可以判断：在 5 秒窗这一时间粒度上，观测到的 Hilbert 相位不服从 '
     'Kuramoto 动力学。因此本复现没有为物理损失增加观测锚定——'
     '把网络约束到一个数据不遵守的方程上，不会产生有效约束。', bold=True)

# ------------------------------------------------------------
h('八、事件级评估', 1)

para('8.1 评估规则', bold=True)
para('逐窗预测需经后处理才能得到报警。本复现采用以下规则，所有阈值在评估前确定：')
bullet('触发条件：任意窗口被预测为类1（发作前期）')
bullet('去抖：容忍 2 个窗口（10 秒）的断点，连续第 3 个非触发窗口才关闭本次报警')
bullet('不应期：30 分钟。不应期内的后续触发不重复报警')
bullet('命中判据：报警起始时刻落在发作前 30 分钟窗口内')

para('8.2 结果', bold=True)
table(
    ['指标', '本复现', '论文报告'],
    [
        ['测试文件数', '9', '—'],
        ['测试总时长', '8.65 小时', '—'],
        ['发作次数', '3', '—'],
        ['报警总次数', '8', '—'],
        ['灵敏度', '3/3', '89.3%'],
        ['误报率（排除发作前窗口）', '0.64 次/小时', '0.48 次/小时'],
        ['误报率（再排除发作中与发作后）', '0.45 次/小时', '0.48 次/小时'],
    ])

para('8.3 对 100% 灵敏度的说明', bold=True)
para('本复现得到 3/3 的灵敏度，但这一结果不具备判别力，'
     '不应被表述为"复现了论文的预测能力"。理由如下：', bold=True)
bullet('模型的类1误报率为 7.5%——在 4692 条正常序列中，有 353 条被判为发作前期。')
bullet('单次发作的预测窗口为 30 分钟，包含约 360 条序列。'
       '在 7.5% 的误报率下，窗口内至少出现一次触发的概率接近 1。')
bullet('换句话说，3/3 是在高误报率下几乎必然发生的事件，而非预测能力的证据。')
para('与之相对，误报率才是有判别力的指标：本复现 0.64 次/小时，'
     '与论文的 0.48 次/小时处于同一量级，略高。')

# ------------------------------------------------------------
h('九、讨论与局限', 1)

para('9.1 复现的主要障碍', bold=True)
para('本次复现耗时最多的部分不是模型本身，而是补全论文缺失的数据管线。'
     'code_2 的缺失使「Hilbert 相位 → PLV → 超边」这一段完全需要自行设计，'
     '其中通道对齐方式、窗口标签定义等关键参数论文均未给出；'
     'τ 阈值论文虽给出（0.65），但按本复现的流程会导致超边过少，'
     '不得不改为 0.55。这意味着本复现得到的数值结果与论文之间，'
     '除了实现差异之外，还存在参数选择上的偏离。')

para('9.2 局限性', bold=True)
bullet('单患者。仅使用 chb01，测试集仅 3 次发作。'
       '事件级指标的 95% 置信区间约为 [30%, 100%]，'
       '无法与论文的 89.3% 做统计比较。')
bullet('误报率受后处理规则影响很大。模型原始触发 745 条序列，'
       '0.64 次/小时这个数字是被 30 分钟不应期压制后的结果，'
       '更换规则会得到不同数值。')
bullet('存在已知的类别混淆。8 次报警中有 2 次紧跟在真实发作后 1 分钟内，'
       '源于类1（发作前期）与类3（发作期）之间的双向混淆。')
bullet('时序模型以 GRU 替代了论文的 Mamba，'
       '超图卷积的具体形式也可能与论文存在差异。')
bullet('Kuramoto 检验仅在 5 秒窗粒度上完成，'
       '且仅覆盖 chb01。更细时间尺度上该方程是否成立，本工作未做检验。')

para('9.3 若继续扩展', bold=True)
para('实测 CHB-MIT 全部 24 个患者后，发现只有 10 个患者的记录为纯 23 通道，'
     '与当前管线的假设一致；其余患者的通道数为 24 至 38 不等，'
     '而管线中多处将 22 硬编码，处理这些记录时会静默取错数据而不报错。')

para('在这 10 个可用患者中，有 5 个不含任何发作事件，对灵敏度评估无用。'
     '推荐组合为 chb01、chb02、chb03、chb05、chb24，'
     '合计 177 个文件、38 次发作（其中 chb24 单独含 16 次）。'
     '相较之下，当前单患者 chb01 仅 7 次发作、测试集 3 次。'
     '扩展后事件级指标才具备统计意义——这是本工作最值得继续的方向。')

# ------------------------------------------------------------
h('十、结论', 1)
para('本工作完成了 HP-GNN 在 CHB-MIT chb01 上的完整复现，'
     '并对其物理信息约束机制做了检验。四条主要结论：')

para('第一，官方源码的物理损失在实现上退化，且与论文正文冲突。导数项被写死为零，'
     '使损失退化为把耦合强度压向零的正则项；而论文方法节明确写的是'
     '「用有限差分近似 dθ/dt」。实测 K 从 0.87 降至 0.22，'
     '落入论文声称奇美拉区间 [0.4–0.9] 之外。', bold=True)

para('第二，论文自报的物理损失效应量小于其自身的折间噪声。'
     '论文原文承认物理约束只带来 +0.9% 准确率提升，而 HP-GNN 的折间标准差为 ±2.1%。'
     '本复现测得 +0.36%，与论文一致。'
     '结论不是「没能复现出该效果」，而是该量级本就落在噪声内。', bold=True)

para('第三，问题不止于实现。对真实 Hilbert 相位的直接拟合显示，'
     'Kuramoto 方程的耦合项在三种脑状态、三个频带、25 个相位延迟下，'
     '对相位演化的方差解释率最高仅 1.55%，发作间期低至 0.00%。'
     '即便完全正确地实现，该损失项也不可能提供有效约束。', bold=True)

para('第四，事件级评估的 100% 灵敏度是平凡的。'
     '模型在正常序列上的误报率为 7.5%，而单次发作的预测窗口包含数百条序列，'
     '命中接近必然。有意义的指标是误报率 0.64 次/小时，'
     '与论文报告的 0.48 次/小时处于同一量级。', bold=True)

para('综合来看，本模型的"物理信息"体现在输入表示——'
     '超图结构由 PLV 这一物理量构建——而不在损失函数。'
     '论文将物理约束押在 Kuramoto 残差上，这一选择在该数据集上缺乏依据。')

doc.add_paragraph()
tail = doc.add_paragraph()
r = tail.add_run('附：《官方源码问题汇总.docx》——官方源码与文献不符之处、'
                 '源码漏洞清单、code_2 缺失的证据链。')
r.font.size = Pt(9.5)
r.italic = True
set_font(r)

doc.save(OUT)
print('已生成：', OUT)
print('段落数：', len(doc.paragraphs), ' 表格数：', len(doc.tables))
